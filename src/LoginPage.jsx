import React, { Component } from 'react';
import LoginForm from './LoginForm.jsx';

export default class LoginPage extends Component {
  render() {
    return (
      <div>
      <div>page for loggin in</div>
      <LoginForm />
    );
  }
}
